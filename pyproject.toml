[project]
name = "attendence"
version = "1.0"
requires-python = ">=3.10"
dependencies = [
    'fastapi[all]'
    'bcrypt'
    'sqlalchemy'
    'alembic'
    'psycopg2-binary'
    'pyjwt'
    'pydantic[email]'
    'pytest'
    'requests'
    'openpyxl'
]
[project.scripts]
app = "app.main:app"
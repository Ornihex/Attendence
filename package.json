{
  "scripts": {
    "build:production": "python -m uvicorn main:app --app-dir app --host 127.0.0.1 --port 8080",
    "build:preview": "python -m uvicorn main:app --app-dir app --host 127.0.0.1 --port 8080"
  }
}